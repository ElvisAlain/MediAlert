<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cmica">
        <activity
            android:name=".GuiasActivity"
            android:exported="false"
            android:label="@string/title_activity_guias"
            android:theme="@style/Theme.Cmica" />
        <activity
            android:name=".BotiquinActivity"
            android:exported="false"
            android:label="@string/title_activity_botiquin"
            android:theme="@style/Theme.Cmica" />
        <activity
            android:name=".NotificacionesActivity"
            android:exported="false"
            android:label="@string/title_activity_notificaciones"
            android:theme="@style/Theme.Cmica" />
        <activity
            android:name=".DatosPersonalesActivity"
            android:exported="false"
            android:label="@string/title_activity_datos_personales"
            android:theme="@style/Theme.Cmica" />
        <activity
            android:name=".GeolocalizacionActivity"
            android:exported="false"
            android:label="@string/title_activity_geolocalizacion"
            android:theme="@style/Theme.Cmica" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Cmica">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>